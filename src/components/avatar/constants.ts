import type { AvatarSize, AvatarShape } from './types';

export const AVATAR_SIZE: Record<AvatarSize, number> = {
  small: 32, // Example: 32px
  medium: 48, // Example: 48px
  large: 64, // Example: 64px
};

export const DEFAULT_AVATAR_SHAPE: AvatarShape = 'circle';
