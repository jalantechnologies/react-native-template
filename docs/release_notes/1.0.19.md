v1.0.19
- UI migrated to React Native Paper with a custom MD3 theme
- Introduced a unified theme for colors and typography matching the existing design system
- Updated form components (buttons, inputs, radio buttons, OTP inputs, form controls) across relevant screens
- Removed legacy custom UI components(inputs, radio buttons, OTP inputs, form controls) previously built on NativeBase after migration