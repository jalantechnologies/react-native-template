## What's New

### Developer Experience
- Reorganized README for clarity and faster onboarding
- Added `.tool-versions` file with pinned Node, Yarn, Ruby, and Bundler versions
- Added Xcode version requirements and installation instructions using xcodes CLI

### Bug Fixes
- Fixed app crash on Android API 36 caused by invalid lineHeight values
- Fixed deprecated `defaultProps` warnings in Brand and FormControl components
- Removed deprecated Flipper hook (removed in React Native 0.73+)
- Removed unused `@react-navigation/devtools` dependency

### UI Fixes
- Fixed "Better." header text being cut off on Android status bar
- Fixed "Login" and "Verify OTP" headings being clipped at the top
- Fixed gear icon alignment in auth screen headers
- Fixed "Privacy Policy" and "Resend OTP" link text alignment
- Improved auth layout reliability by using native React Native components
