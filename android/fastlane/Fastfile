# This file contains the fastlane.tools configuration
# You can find the documentation at https://docs.fastlane.tools
#
# For a list of all available actions, check out
#
#     https://docs.fastlane.tools/actions
#
# For a list of all available plugins, check out
#
#     https://docs.fastlane.tools/plugins/available-plugins
#

# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

default_platform(:android)

platform :android do

  desc "Fetches the latest version code from the Play Console and increments it by 1"
  lane :fetch_and_increment_build_number do
    app_identifier = CredentialsManager::AppfileConfig.try_fetch_value(:app_identifier)

    version_codes = google_play_track_version_codes(
      package_name: app_identifier,
      track: "internal",
      json_key: ENV["ANDROID_JSON_KEY_FILE"]
    )
    
    updated_version_code = version_codes[0] + 1
    
    increment_version_code(
      version_code: updated_version_code
    )
    updated_version_code
  end

  desc "Deploy a new version to the Google Play (Production)"
  lane :deploy_android_production do
    # Capture returned versionCode from fetch lane
    updated_version_code = fetch_and_increment_build_number

    # Build AAB/APK with signing
    gradle(
      task: "bundle",
      build_type: "release",
      properties: {
        "android.injected.signing.store.file" => ENV["ANDROID_KEYSTORE_FILE"],
        "android.injected.signing.store.password" => ENV["ANDROID_KEYSTORE_PASSWORD"],
        "android.injected.signing.key.alias" => ENV["ANDROID_KEY_ALIAS"],
        "android.injected.signing.key.password" => ENV["ANDROID_KEY_PASSWORD"]
      }
    )

    # Read release notes text (directly from env)
    release_notes = ENV["RELEASE_NOTES"]
    release_notes_language = "en-US"

    # Create changelog file only if notes exist
    if release_notes && !release_notes.strip.empty?
      changelog_dir = "fastlane/metadata/android/#{release_notes_language}/changelogs"
      changelog_path = "#{changelog_dir}/#{updated_version_code}.txt"

      FileUtils.mkdir_p(changelog_dir)
      File.write(changelog_path, release_notes)

      UI.success("📝 Created Play Store changelog: #{changelog_path}")
    else
      UI.important("⚠️ No release notes provided — skipping changelog generation")
    end

    # Upload only changelogs (NOT full metadata)
    upload_to_play_store(
      track: "internal",
      release_status: "draft",
      json_key: ENV["ANDROID_JSON_KEY_FILE"],
      skip_upload_apk: true,
      skip_upload_metadata: true,      # do not upload title, desc etc
      skip_upload_changelogs: false    # ensure changelogs ARE uploaded
    )
  end


  desc "Deploy APK to Firebase App Distribution (PR Preview)"
  lane :pr_deploy do |options|
    # Load the deploy script
    load './scripts/firebase_pr_deploy.rb'
    firebase_pr_deploy(
      pr_number: options[:pr_number],
      pr_title: options[:pr_title],
      project_number: options[:project_number],
      app_id: options[:app_id],
      service_account_path: "/tmp/gcp_key.json",
      release_notes: ENV["RELEASE_NOTES"] 
    )

  end


  desc "Clean up Firebase App Distribution releases for closed PR"
  lane :pr_cleanup do |options|
    # Load the cleanup script
    load './scripts/firebase_pr_cleanup.rb'
    firebase_pr_cleanup(
      pr_number: options[:pr_number],
      project_number: options[:project_number],
      app_id: options[:app_id],
      service_account_path: "/tmp/gcp_key.json" # Path to GCP service account key
    )
  end
end
